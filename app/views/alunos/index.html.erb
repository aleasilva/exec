<h1>Listing alunos</h1>
<%# show_filters: :when_filtered %>
<%= grid(@alunos_grid) do |g|
	
	g.column name:  'Cód.Aluno', attribute: 'idAcademia' do |aluno|
		aluno.idAcademia
	end
	
	g.column name:  'Nome', attribute: 'nome'  do |aluno|
		aluno.nome
	end

	g.column   do |aluno|
		link_to('Ver', aluno)
	end
	
	g.column   do |aluno|
		link_to('Altera', edit_aluno_path(aluno))
	end
	
	g.column   do |aluno|
		link_to 'Exclui', aluno, method: :delete, data: { confirm: 'Are you sure?' }
	end
end %>
<%= link_to 'Novo Aluno', new_aluno_path %>
