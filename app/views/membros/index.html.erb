<h1><%= t(".title") %></h1>

<table>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @membros.each do |membro| %>
  <tr>
    <td><%= membro.nome %></td>
    <td><%= link_to 'Show', membro %></td>
    <td><%= link_to 'Edit', edit_membro_path(membro) %></td>
    <td><%= link_to 'Destroy', membro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Partescorpo', new_membro_path %>
