<%= form_for(@tipovenda) do |f| %>
  <% if @tipovenda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipovenda.errors.count, "error") %> prohibited this tipovenda from being saved:</h2>

      <ul>
      <% @tipovenda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
