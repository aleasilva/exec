<%- model_class = Vendaplano -%>

<dl class="dl-horizontal">
  <dt><strong>Aluno:</strong></dt>
  <dd><%= @vendaplano.aluno.nome %></dd>

  <dt><strong>Tipo do plano:</strong></dt>
  <dd><%= @vendaplano.nome_plano %></dd>

  <dt><strong>Forma pagamento:</strong></dt>
  <dd><%= @vendaplano.formapagamento.nome  %></dd>

  <dt><strong>Tipo pagamento</strong></dt>
  <dd><%= @vendaplano.tipovenda.nome %></dd>

  <dt><strong>Data venda:</strong></dt>
  <dd><%= l(@vendaplano.dt_venda) %></dd>

</dl>

<div class="panel-footer">
  <%= link_to t('.back', :default => t("helpers.links.back")), vendaplanos_path, :class => 'btn btn-primary'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_vendaplano_path(@vendaplano), :class => 'btn btn-primary' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")), vendaplano_path(@vendaplano),
              :method => 'delete', :data => { :confirm => t('.confirm', :default => t("helpers.links.confirmdelete", :default => 'Você tem certeza que deseja excluir o registro?')) },
              :class => 'btn btn-danger' %>
</div>
